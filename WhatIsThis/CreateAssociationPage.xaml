<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WhatIsThis.Views.CreateAssociationPage"
             Title="Créer une Association">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Entry
                x:Name="associationEntry"
                Placeholder="Ecrire le mot"
                Text="{Binding Word}"
                HorizontalOptions="Center"
                ReturnType="Done" />

            <Image
                Source="{Binding ImageSource}"
                IsVisible="{Binding ShowImage}"
                HeightRequest="200"
                HorizontalOptions="Center" />

            <Button
                Text="Choisir l'image"
                HorizontalOptions="Center"
                Command="{Binding OnImageChosenCommand}" />

            <Button
                Text="Sauvegarder l'association"
                HorizontalOptions="Center"
                Command="{Binding OnSaveCommand}" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
